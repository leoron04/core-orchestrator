[tool.black]
line-length = 100
target-version = ["py311"]

[tool.ruff]
line-length = 100
target-version = "py311"
select = ["E", "F", "I", "B", "UP"]
ignore = ["E203"]

[tool.pytest.ini_options]
addopts = "-q --cov=core --cov=ai --cov=modules --cov-report=term-missing"
pythonpath = "."
